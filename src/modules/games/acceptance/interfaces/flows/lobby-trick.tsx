import { ChangeTrick } from '../slices/change-trick';

export const LobbyTrickFlow = () => {
  return (
    <div className='max-desktop-xs:items-center flex w-full flex-col gap-2.5'>
      <ChangeTrick />
    </div>
  );
};
